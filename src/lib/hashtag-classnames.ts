/** In-app #tag link styling (shared by post lists and the tag index). */
export const hashtagClassNames = {
  link: "text-xs font-mono text-base-content/40 hover:text-primary/70 transition-colors",
  linkActive: "text-xs font-mono text-primary",
} as const;
